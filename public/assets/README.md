# Dustaway images (transparent background)

Place your **transparent-background** PNGs here so the site shows only the character/illustration with no visible box.

- `dustaway-hero.png` — Hero section (friendly cleaning character)
- `dustaway-clean-home.png` — About section (sparkling home)
- `dustaway-mascot.png` — Optional mascot

**If you see a checkerboard behind the image:** that usually means the PNG has the checkerboard pattern baked into the file (from an editor’s “transparency” preview) instead of real transparency. Replace the file with a version exported with **transparent background** (alpha channel, no background layer).

AI-generated versions with transparent background were created in:
`C:\Users\rodeguzman\.cursor\projects\c-Users-rodeguzman-dustaway\assets\`

Copy those three files into this folder to use them on the site.
